// Intentionally minimal — the wrapped site needs no special bridged APIs today.
// contextIsolation stays on (Electron default) for security; add contextBridge
// exposures here only if a future feature genuinely needs main-process access.
